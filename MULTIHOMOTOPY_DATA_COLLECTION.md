# 多拓扑 SE(3) 数据采集设计

## 为什么旧数据只有“纺锤形”差异

旧地图的门廊中心位于 `y=-2.45 m`，而可采样机体中心的南侧边界约为
`y=-2.723 m`，门后只有约 `0.27 m` 的端点空间。随机起终点很少真正跨越门廊。
旧采集器对已有路径添加一个连续中间引导点，并用轨迹 RMS 距离验收多样性；
这会制造连续形变，但不会主动改变障碍物绕行侧，因此 32 条旧数据中每个起终点组
都只有一个门廊/中央分隔通道签名。

## v002 地图与任务分布

`environment_multihomotopy_v002` 做了两项配套调整：

- 门廊移到场地内部 `y=-1.35 m`，南北两侧都有完整任务空间；
- 起点从南侧 pose region 随机采样，终点从北侧 pose region 随机采样，保证每个任务
  都需要处理门廊与中央高障碍，而不是偶然碰到它们。

默认每组四条轨迹覆盖：

1. 穿门后从中央障碍左侧通过；
2. 穿门后从中央障碍右侧通过；
3. 从门左侧绕行，再从中央障碍左侧通过；
4. 从门右侧绕行，再从中央障碍右侧通过。

地图还提供两个显式的门梁上绕模板。设置
`--trajectories-per-pair 6` 时会加入“上绕后向左/右分流”；默认四条不包含它们，
避免训练分布被上绕策略主导。

当每组数量超过 6、模板开始循环时，采集器还会对同一通道类恢复位置/姿态 RMS
多样性门槛；因此新增样本既不能走错类别，也不能只是已有同类轨迹的近似副本。

这里的“拓扑类别”是由两个分隔切面上的通道签名定义的实用代理，并非一般三维
构型空间同伦类的完备数学判定；对于这张由门廊和中央分隔体构成的地图，它能稳定
区分任务所关心的绕行类别。

## 规划、软引导与无碰撞保证

每个类别在切面前后各放置一对随机 SE(3) 门户位姿。门户位姿是硬约束，不是代价项。
五个连续子段分别具有位置域，例如门前子段不能提前跨门、穿门子段只能处于指定
门户、中央穿越子段只能位于指定左右侧。每个子段独立运行 OMPL RRTConnect，并由
完整 URDF 的 COAL 碰撞模型做状态有效性检查。

拼接后的 RRT 状态是全局 SE(3) B 样条优化的软引导：优化目标惩罚相对引导的偏离、
位置/姿态加速度和 jerk；靠近障碍的引导样本权重更高。软引导允许曲线消除折角，
但不会单独承担安全保证。安全性来自以下硬验收链：

- B 样条必须精确通过所有门户位姿；
- B 样条在每个门户参数区间内不得离开对应子段位置域；
- 稠密 B 样条必须通过带安全缓冲的 COAL 碰撞检查；
- TOPP-RA 只沿已验收的几何曲线分配时间；
- MPPI/MuJoCo 实际轨迹再次用零缓冲物理模型审计；
- 规划轨迹与实际轨迹的两个切面签名都必须等于目标类别。

因此，RRT 或平滑器走错通道、B 样条切角、跟踪碰撞都会导致该次尝试被剔除并换种子，
不会进入训练集。

## 对 mpd-splines-public 的取舍

采用了该库适合本任务的高层思路：从任务 pose region 采样、同一任务运行多次独立
RRTConnect、先保存原始规划再统一后处理、按任务分组数据。没有照搬其 PyBullet
碰撞后端、机械臂关节空间参数或仅靠重复随机种子制造多样性的设置；当前任务使用
COAL 的浮动基座完整 URDF 碰撞、显式通道覆盖以及 MuJoCo 闭环执行验收。

## 运行方式

默认正式采集（8 组起终点，每组 4 个主要类别）：

```bash
./collect_diffusion_dataset.sh \
  --output-dir datasets/diffusion_se3_multihomotopy_v002
```

包含门梁上绕类别：

```bash
./collect_diffusion_dataset.sh \
  --pair-count 8 \
  --trajectories-per-pair 6 \
  --output-dir datasets/diffusion_se3_multihomotopy_v002_six_classes
```

静态总览：

```bash
.venv/bin/python visualize_diffusion_dataset.py \
  --dataset-dir datasets/diffusion_se3_multihomotopy_v002
```

## 已完成的 pilot

`datasets/diffusion_se3_multihomotopy_v002_pilot` 使用正式默认的 384 个 MPPI 样本，
采集了 2 组随机起终点、每组 4 类，共 8 条轨迹。两组均有 4 个唯一实际通道签名；
位置 RMSE 为 `0.0224–0.0273 m`，实际轨迹最小物理间隙为 `0.0571–0.0873 m`。
