# SE(3) 多同伦 Diffusion 数据与训练实验报告

## 结论

本轮已经完成 300 条高质量规划/跟踪数据、三个独立模型检查点和四个统一测试方案。当前固定地图上的推荐方案是 **U-Net + inference-time guidance + COAL 过滤**：在 6 个从未参与训练的起终点对、每对 32 次采样上，单条物理无碰撞率为 29.7%，8 cm 安全率为 15.1%；每个测试任务都至少得到一条 8 cm 安全路径，并且每个任务恢复至少 2 种、平均 3.67 种无碰撞拓扑。

这仍然是候选生成器，不是可以绕过碰撞检查的端到端安全规划器。所有输出必须经过完整 URDF 的 COAL 精确检查，只有通过 8 cm 门槛的候选才进入 B 样条重拟合、再次连续碰撞审计、TOPP-RA 和 MPPI/MuJoCo 执行链。

## 数据集

- 50 个独立随机起终点对，起点来自当前南侧区域，终点来自北侧区域。
- 每对严格包含 6 条轨迹，对应 `gate_cut × center_cut` 的 6 种指定拓扑；每类恰好 50 条。
- 总计 300 条；无拓扑错配，无 `diversity_relaxed` 样本。
- 84 次规划/平滑候选因质量门槛被拒绝并重试，未进入训练集。
- 规划路径长度均值 7.949 m，5%–95% 为 6.462–9.665 m。
- MPPI 跟踪位置 RMSE 均值 2.50 cm，姿态 RMSE 均值 1.72°。
- 训练所用平滑参考路径的物理净空最小值 10.00 cm，均值 10.21 cm。
- 转为训练用 128 个 SE(3) 节点后，300/300 仍无碰撞、满足 8 cm 且拓扑不变；压缩后的最小 COAL 净空为 9.23 cm。

按起终点对而不是按轨迹随机切分：38 对训练、6 对验证、6 对测试。只有训练集做正反向增强，所以模型实际看到 456 个训练样本；验证和测试各 36 条，避免同一起终点的轨迹同时出现在训练和测试中。

## 训练表示与共同设置

路径表示为 `128 × (xyz + SO(3) 6D)`；6D 旋转在输出时投影回合法旋转矩阵和四元数。起终位姿作为 18 维条件输入，并在加噪和每个 DDIM 步骤中硬固定。训练目标是 velocity prediction，使用 100 步 cosine diffusion；推理使用 25 步确定性 DDIM。

三个模型均使用 AdamW、batch 64、峰值学习率 `2e-4`、200 步 warmup、cosine decay、EMA 0.995、混合精度和基于验证损失的最佳检查点。四种方案使用完全相同的测试起终点、32 个样本数和初始高斯噪声流。

参考 MPD 的部分包括：按 planning task 分组切分、端点硬条件、训练先验与推理期代价引导分离、后段 DDIM 引导、扰动裁剪以及最终精确碰撞过滤。没有直接照搬其关节空间 B 样条控制点设置，因为这里是带姿态的刚体 SE(3) 任务，且现有轨迹的控制点数量并不固定。

## 三个训练阶段与四种评测方案

1. `unet_no_guidance`：一维条件 U-Net，含多尺度卷积、FiLM 条件和 bottleneck self-attention。
2. `unet_guidance`：不训练新模型，复用阶段 1 的同一检查点，只在 DDIM 最后 40% 步骤注入碰撞、平滑、长度和边界代价梯度。
3. `dit_no_guidance`：将 U-Net 替换为 4 层、128 维、4 头的 DiT；起终点和时间通过 adaptive modulation 注入。
4. `dit_cross_environment`：在 DiT 每个 block 中加入路径 token 到环境 box token 的 cross-attention，环境 token 为 `center + size + quaternion` 并带 padding mask。

因此严格说是三个独立训练检查点、四个推理方案；guidance 是推理策略而不是第四个模型。

## Guidance 的实现与安全边界

可微碰撞代理与 URDF 的 active collision primitives 对齐：1 个机身 OBB、2 个旋翼球和 4 个有限圆柱，对环境 OBB 使用球-盒距离和 separating-axis 支持函数。旧的五大球近似会把 80.7% 的安全专家轨迹误判为碰撞，已经删除。新代理把 96.3% 的专家轨迹判为物理可行，并通过自动审计防止该问题回归。

验证集比较了三档参数，最终选择后 40% DDIM、步长 0.02、每步 2 次、最大局部扰动 0.12、代理净空目标 6 cm。代理相对 COAL 偏保守，6 cm 是针对真实 8 cm 门槛的验证集标定值。端点梯度恒为零，每个样本的梯度按 RMS 归一化，更新被局部裁剪。

Guidance 只提高找到安全候选的概率，不能构成安全证明。最终指标全部来自完整 URDF + COAL，未使用代理碰撞值打分。

## 测试集对比

每种方案 192 条路径（6 对 × 32）；`Task solved` 表示某起终点的 32 条中至少一条物理无碰撞，`8 cm task` 对应至少一条满足规划安全裕度。

| 方案 | 物理无碰撞 | 95% pair-bootstrap CI | 8 cm 安全 | Task solved | 8 cm task | 已知拓扑 | 无碰撞拓扑/对 | 最少拓扑/对 | 最近专家 RMS | 长度 | 推理延迟 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| U-Net，无引导 | 4.2% | 1.6%–6.8% | 0.0% | 4/6 | 0/6 | 90.6% | 1.17 | 0 | 0.667 m | 11.399 m | 0.8 ms |
| U-Net，guidance | **29.7%** | **21.9%–37.0%** | **15.1%** | **6/6** | **6/6** | 95.3% | **3.67** | **2** | 0.664 m | 11.877 m | 41.3 ms |
| DiT，无引导 | 6.8% | 3.6%–9.9% | 0.5% | 6/6 | 1/6 | **95.8%** | 1.83 | 1 | **0.473 m** | **8.166 m** | **0.7 ms** |
| DiT + 环境 cross-attention | 2.6% | 0.5%–4.7% | 1.0% | 3/6 | 2/6 | 95.3% | 0.67 | 0 | 0.511 m | 8.907 m | 1.0 ms |

配对任务 bootstrap 显示 guidance 相对同权重 U-Net 的物理无碰撞率增益为 +25.5 个百分点，95% CI 为 +20.3 至 +30.2 个百分点；已知拓扑率增加 +4.7 个百分点。DiT 相对 U-Net 的路径更接近专家（RMS 降低 0.194 m）、更短、更快，但安全裕度仍不足。

## 模型效率

| 检查点 | 参数量 | 最佳步 | 最佳验证 velocity MSE | 训练时间 |
|---|---:|---:|---:|---:|
| U-Net | 2,349,769 | 3000 | 0.15405 | 1.19 min |
| DiT | 1,282,953 | 4000 | 0.13139 | 1.16 min |
| DiT + cross-attention | 1,565,065 | 3750 | 0.13512 | 1.37 min |

## 如何解释结果

- 当前最实用的是 U-Net guidance：32 次并行采样后以 COAL 过滤，测试集 6/6 都有 8 cm 安全候选，并保留较好的多同伦覆盖。
- 纯 DiT 是更好的快速先验：验证损失、专家距离、路径长度和推理速度均优于 U-Net，但必须增加可靠的碰撞引导或后处理才能用于执行。
- 环境 cross-attention 已完成架构和数据接口验证，但当前只有一张固定地图，所有样本的环境 token 完全相同。它无法从本数据中学习“地图变化如何改变路径”，本轮性能下降不能证明 cross-attention 无效，只能说明在单地图 300 条数据上增加该分支没有收益。
- 测试集只有 6 个独立起终点任务，虽然每个任务有 32 条样本并使用 task-cluster bootstrap，统计区间仍是初步结果；下一轮应优先增加独立起终点对，而不是只增加同一对的重复轨迹。

## 建议的实际调用策略

1. 对新起终点并行生成 32 条 U-Net guidance 候选。
2. 用完整 URDF + COAL 加密检查全部候选，拒绝任何越界、碰撞或小于 8 cm 的路径。
3. 在安全候选中先按拓扑去重，再按净空、长度、曲率/加速度综合排序。
4. 对选中的路径进行受端点约束的 B 样条重拟合，并再次做连续 COAL 审计。
5. 通过 TOPP-RA 分配时间，交给现有 MPPI/MuJoCo 链执行；执行失败数据不能反向标成成功专家。
6. 如果 32 条均未通过，回退到现有 OMPL bi-RRT/多同伦规划器，而不是放宽碰撞门槛。

若下一阶段目标是跨地图泛化，应生成多个参数化地图/障碍布局，每个地图保留环境 token、起终点和多拓扑轨迹，并按地图分组做 train/validation/test；届时再比较 DiT 与 cross-attention 才具有环境条件泛化意义。
